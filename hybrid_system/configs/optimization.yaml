target: hybrid_system/smile.png
# Omit init_phase (or set null) for uniform random phase on the DOE aperture.
init_phase: null
results_dir: outputs/optimization
epochs: 100
steps_per_epoch: 32
lr: 10.0
seed: 1234
two_pass: true
device: auto
dtype: float32
